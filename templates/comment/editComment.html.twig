{% extends 'base.html.twig' %}

{% block title %} Edit comment {% endblock %}

{% block body %}
    <div class="form">
        {{ form_start(comment_edit_form) }}
        <div>
            {{ form_row(comment_edit_form.content) }}
        </div>
        {{ form_end(comment_edit_form) }}
    </div>
{% endblock %}